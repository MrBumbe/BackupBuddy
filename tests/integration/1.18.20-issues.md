# 1.18.20 — Issues log

> Second three-user install-and-restore simulation.
> Create this file at the start of the test run, even if empty. Update throughout.

---

Format for each issue:

```
## ISSUE-NNN
Step: <section letter + number>
Symptom: <what happened>
Blocking: yes / no
Fix applied (if blocking): <what was done; if yes, nodes rolled back and test restarted from A1>
```

---

## ISSUE-001
Step: C2 (install)
Symptom: TODO.md 1.18.20 §C2 shows install URL `https://raw.githubusercontent.com/johankyrkjerod/backupbuddy/master/install/gatekeeper.sh` — this repo/user does not match the actual repo (MrBumbe/BackupBuddy). Using correct INSTALL.md §3 procedure instead: `git clone https://github.com/MrBumbe/BackupBuddy.git /opt/backup-buddy && sudo bash /opt/backup-buddy/install/gatekeeper.sh`.
Blocking: no

## ISSUE-002
Step: A1 (rollback)
Symptom: VM 101 (gatekeeper-anders) clean-ubuntu snapshot description says "wizard not run" but after rollback the service runs in normal mode with full wizard state — gatekeeper.cfg, catalog.db, root_dir.cap, recovery_kit.enc all present. Snapshot was taken on 2026-06-04 09:13:14 during task 1.18.11, apparently after a wizard run rather than before.
Blocking: no — resetting wizard state by deleting config/data files on VM 101 to force setup mode, then running install script.

## ISSUE-003
Step: C2 (install) and D wizard
Symptom: All 20+ 1.18.x commits (1.18.2–1.18.13 fixes) were committed locally but never pushed to GitHub. When VMs 101/102/103 run `git clone https://github.com/MrBumbe/BackupBuddy.git`, they get commit e2d8013c8 (2026-06-02) — missing all 1.18.x fixes. Björn's wizard step 3 failed with "Path does not exist" confirming the 1.18.3 auto-create fix is not deployed. The test cannot verify 1.18.x fixes with old code.
Blocking: YES
Fix applied: Committed current test state files + pushed all pending commits to GitHub (20 commits, latest: marks 1.18.11-1.18.13 done). All 6 nodes rolled back to clean-ubuntu and test restarted from A1.
