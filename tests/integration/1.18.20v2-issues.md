# 1.18.20v2 — Issues log

> Second three-user install-and-restore simulation — re-run after fixes 1.18.23–1.18.26.
> Create this file at the start of the test run, even if empty. Update throughout.

---

Format for each issue:

```
## ISSUE-NNN
Step: <section letter + number>
Symptom: <what happened>
Blocking: yes / no
Fix applied (if blocking): <what was done; if yes, nodes rolled back and test restarted from A1>
```

---

## ISSUE-001
Step: C5 (Anders wizard step 5 — cascade new cluster)
Symptom: First invite code `bolt-herbs-8` was generated in _cascade_new_cluster() and recorded
in onboarding_state.json, but not persisted to cluster.db. After wizard completion and service
restart, the invites table had 0 rows. Björn's cascade join using bolt-herbs-8 received HTTP 400
"Join rejected" from Anders. Anders member (anders-home) was correctly inserted in the same
transaction, so the DB connection itself was functional.
Root cause: unknown — needs investigation. The `generate_invite()` call commits, and the member
insertion committed correctly. Could be WAL checkpoint issue or a code path that clears invites.
Blocking: no — generated new invite via POST /api/buddies/invite (slit-fled-9). Björn joined
successfully on second attempt.
